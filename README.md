# DataVault-Record-Manager
DataVault is a lightweight employee record manager I built with pure HTML, CSS, and JavaScript — no frameworks or backend needed. It handles full CRUD, real-time search, filtering, sorting, and CSV export, all wrapped in a clean dark-themed UI that runs entirely in the browser.
